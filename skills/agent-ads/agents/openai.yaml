interface:
  display_name: "Agent Ads"
  short_description: "Provider-first ads CLI skill"
  default_prompt: "Use $agent-ads to inspect providers, check auth status, and run explicit Meta, Google, TikTok, Pinterest, LinkedIn, or X CLI commands with provider prefixes."

policy:
  allow_implicit_invocation: true
